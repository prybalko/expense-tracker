services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./data:/app/data
    ports:
      - "8080:8080"
    environment:
      - DB_PATH=/app/data/expenses.db
    restart: unless-stopped
